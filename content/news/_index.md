---
title: "News"
# No standalone /news/ page — news is rendered inline on the About page via the
# {{< news >}} shortcode. Individual announcements still get their own pages so
# the longer ones (with photos) remain linkable.
build:
  render: never
  list: local
---
